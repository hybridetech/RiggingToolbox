/*
 *  Copyright 2010-2014 Fabric Engine Inc. All rights reserved.
 */

require Math;
require Geometry;

interface CachePoint  {


	// Temp: Used in debugging and unit testing.
	disable!();
	enable!();

	invalidate!();
	Boolean isValid();
	init!(GeometrySet geomSet);
	restore(io GeometrySet geomSet);
	update!(io GeometrySet geomSet);
};


 